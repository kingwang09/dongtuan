define(["jquery","bootstrap/dialog"],function($){
	
	return function(spec){
		var dialogLinkId = spec.dialogLinkId;
		var dialogId  = spec.dialogId;
		
		$("#"+dialogLinkId).attr("data-toggle","modal");
		$("#"+dialogLinkId).attr("data-target","#"+dialogId);
	};
});